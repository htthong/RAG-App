# RAG-App
Simple system using RAG 

(Default) Use Gemini
Set up file secrets.yaml(example in sample_secrets.yaml)

Install requirement packages
$ pip install -r requirement

Set INIT_INDEX = true
$ set INIT_INDEX=true

Run application
$ python api.py